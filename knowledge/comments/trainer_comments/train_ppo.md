# train_ppo.py 注释整理

> 本文档收录 `scripts/Trainer/train_ppo.py` 中被移除的全部注释与 docstring。
> 按原代码顺序分节，每节对应原代码中的一个逻辑块 / 函数。

---

## 模块头部

> 对应原代码：第 1–28 行（导入区及模块说明）

**注释：**

```text
自定义的Critic模型，继承自MiniMindLM
```

---

## 函数 `__init__`

> 对应原代码：`def __init__():`（第 30–34 行）

**注释：**

```text
替换lm_head为输出单一价值的线性层
```

---

## 函数 `forward`

> 对应原代码：`def forward():`（第 35–43 行）

**注释：**

```text
使用基础模型获取隐藏状态
使用value_head获取价值估计
```

---

## 函数 `calculate_rewards`

> 对应原代码：`def calculate_rewards():`（第 44–45 行）

**docstring：**

```text
整合所有奖励函数计算总奖励
```

---

## 函数 `reasoning_model_reward`

> 对应原代码：`def reasoning_model_reward():`（第 46–64 行）

**注释：**

```text
1. 格式奖励（仅针对训练推理模型时使用）
2. 标记奖励（防止严格奖励稀疏，仅针对训练推理模型时使用）
```

---

## 函数 `mark_num`

> 对应原代码：`def mark_num():`（第 65–118 行）

**注释：**

```text
格式奖励
使用reward model计算整个response的奖励
当args.reasoning=1时，额外计算<answer>内容的奖励
对answer内容单独计算reward
```

---

## 函数 `ppo_train_epoch`

> 对应原代码：`def ppo_train_epoch():`（第 119–283 行）

**注释：**

```text
list[str], length B
input_ids: [B, P], attention_mask: [B, P]
DDP 模型需要使用 .module 访问 generate 方法
[B, P+R]
[B]
[B, P+R]
[B, P+R]
[B]
[B]
[B, P+R, V]
[B, P+R-1]
[B, P+R-1]
[B, P+R-1]
[B]
[B, P+R, V]
[B, P+R-1]
[B]
[B, P+R, V]
[B, P+R-1]
[B]
scalar
scalar
[B]
[B]
[B]
scalar
scalar
scalar
使用 lm_checkpoint 保存完整状态（包括 critic）
```

---

## 1. 初始化环境和随机种子

> 对应原代码：`# ========== 1. 初始化环境和随机种子 ==========` 段（第 284–288 行）

**注释：**

```text
========== 1. 初始化环境和随机种子 ==========
```

---

## 2. 配置目录、模型参数、检查ckp

> 对应原代码：`# ========== 2. 配置目录、模型参数、检查ckp ==========` 段（第 289–293 行）

**注释：**

```text
========== 2. 配置目录、模型参数、检查ckp ==========
```

---

## 3. 设置混合精度

> 对应原代码：`# ========== 3. 设置混合精度 ==========` 段（第 294–298 行）

**注释：**

```text
========== 3. 设置混合精度 ==========
```

---

## 4. 配wandb

> 对应原代码：`# ========== 4. 配wandb ==========` 段（第 299–307 行）

**注释：**

```text
========== 4. 配wandb ==========
```

---

## 5. 初始化模型和数据

> 对应原代码：`# ========== 5. 初始化模型和数据 ==========` 段（第 308–344 行）

**注释：**

```text
========== 5. 初始化模型和数据 ==========
Actor模型
Old Actor模型
Reference模型
Critic模型
Reward模型
数据和优化器
```

---

## 6. 从ckp恢复状态

> 对应原代码：`# ========== 6. 从ckp恢复状态 ==========` 段（第 345–356 行）

**注释：**

```text
========== 6. 从ckp恢复状态 ==========
```

---

## 7. DDP包模型

> 对应原代码：`# ========== 7. DDP包模型 ==========` 段（第 357–364 行）

**注释：**

```text
========== 7. DDP包模型 ==========
```

---

## 8. 开始训练

> 对应原代码：`# ========== 8. 开始训练 ==========` 段（第 365–379 行）

**注释：**

```text
========== 8. 开始训练 ==========
```

---

## 9. 清理分布进程

> 对应原代码：`# ========== 9. 清理分布进程 ==========` 段（第 380–381 行）

**注释：**

```text
========== 9. 清理分布进程 ==========
```
